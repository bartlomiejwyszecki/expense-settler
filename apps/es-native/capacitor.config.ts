import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'es-native',
  webDir: '../../dist/apps/es-native',
  bundledWebRuntime: false,
};

export default config;
